- content_for :page_title do
  = t('admin.archives.add_new')

%p= t('admin.archives.description_html')

%hr.spacer/

= simple_form_for [:admin, @archive] do |form|
  = render 'shared/error_messages', object: @archive

  = render form

  .actions
    = form.button :button, t('admin.archives.add_new'), type: :submit
